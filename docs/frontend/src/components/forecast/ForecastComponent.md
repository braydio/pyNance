# ForecastComponent

## Purpose

`ForecastComponent` is a thin wrapper that renders the forecast layout used by the forecast page entry.

## Usage

```vue
<ForecastComponent />
```

## Related Components

- `ForecastLayout` orchestrates the data flow and child components.
